DATA patterns:

mission.create:	type_name_score_period
mission.delete:	missionid
mission.alter: missionid_newtype_newname_newscore_newperiod
mission.consult: "all"

*****************************************************************************

以上格式很整齐，然而这并不是设置文件，只是个说明文件。
为什么不在客户端里让用户依次输入各数据然后再拼合起来呢？
因为那样虽然看着很清楚，实际上用户输入会觉得很麻烦啊。
